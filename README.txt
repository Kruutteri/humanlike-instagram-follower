pip install pywin32
pip install pypiwin32
pip install keyboard
pip install pillow
pip install pyautogui

Open instagram in your browser
Press f12
click on the little Ipad/phone icon in the top left corner
refresh the page
find a post
click on the posts likes
run the code

Hold "l" if you want to stop the code
